#include<stdio.h>

//defining the struct with 3 elements id, course code and status
typedef struct{ 
    int id;
    char courseCode[20];
    char status[10];
} Course;

// create and write a file
void saveToAFile(){
    
}

int main(){


    return 0;
}